export const setEdiblePlants = (plants) => ({
  type: 'SET_EDIBLE_PLANTS',
  plants
});

export const addPlant = (plants) => ({
  type: 'ADD_PLANT',
  plants
});

export const removePlant = (plants) => ({
  type: 'REMOVE_PLANT',
  plants
});


